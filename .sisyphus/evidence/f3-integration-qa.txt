F3 Automated Integration QA Replay - Code-Level Structural Verification
Date: 2026-02-26
Scope: Structural wiring verification only (no runtime browser E2E)

Build/Type Verification
- [PASS] webview/shared: `npx tsc --noEmit` completed with zero reported errors.
- [PASS] project root: `npm run compile` completed and emitted "Build complete!".

Scenario Verification (8/8)
1) Chat send flow (typed -> send -> thread render -> clear, with images)
- [PASS] Input posts `sendMessage` with `text/files/contexts/images/agent` from `InputWrapper`.
- [PASS] Optimistic user render appends message to store and clears input + attachments (`SET_MESSAGES`, `SET_INPUT_VALUE`, `CLEAR_ATTACHMENTS`).
- [PASS] Provider handles `sendMessage` in `ChatViewProvider.onDidReceiveMessage` and persists user message via `sessionService.appendMessage` including `images`.
- [PASS] Assistant response returned as `messageResponse`; webview handler appends to thread.
- [PASS] User image thumbnails rendered in `UserMessage` when `message.images` exists.

2) Plan detection (assistant response -> enrich -> plan button shown)
- [PASS] `ChatViewProvider.enrichMessageWithPlan` exists and attaches `plan: { file, content }` when plan markers match.
- [PASS] Enriched message path used on both load (`handleLoadSession`) and fresh response (`handleSendMessage`).
- [PASS] `AssistantMessage` conditionally renders header "View Plan" button and plan card when `message.plan` exists.

3) Plan viewer (button click -> provider opens panel -> `__PLAN_DATA__` injected)
- [PASS] Webview button posts `{ type: 'viewPlan', plan }`.
- [PASS] Provider `viewPlan` branch resolves content/file and calls `opencode.showPlan`.
- [PASS] `extension.ts` registers `opencode.showPlan` -> `PlanViewProvider.show(...)`.
- [PASS] Plan webview HTML includes `window.__PLAN_DATA__ = ...` and `<div id="root"></div>` for React mount.

4) Comments flow (select text -> add comment UI -> submit -> comments panel updates)
- [PASS] `PlanShell` computes selection anchors from `window.getSelection()` and sets pending comment state.
- [PASS] Add comment posts `{ type: 'addComment', comment }` to extension.
- [PASS] `PlanViewProvider` handles `addComment/updateComment/deleteComment` and posts `commentsUpdated` back.
- [PASS] `PlanShell` listens for `commentsUpdated` and re-renders right-side comments list.

5) Proceed flow (Proceed -> message chain -> file write -> "Proceed" send -> close panel)
- [PASS] `PlanShell` posts `{ type: 'proceedWithPlan', rawPlan, comments }`.
- [PASS] `PlanViewProvider` maps to command `opencode.planProceed`.
- [PASS] `extension.ts` command registration calls `chatViewProvider.handlePlanProceed(payload)`.
- [PASS] `handlePlanProceed` writes `implementation_plan.md`, sends `handleSendMessage("Proceed", [planFilePath])`, then closes panel via `PlanViewProvider.closeCurrentPanel()`.

6) Image attachment flow (paste image -> chip -> send -> thumbnail)
- [PASS] `InputWrapper` `onPaste` detects clipboard image items, creates data URL attachment, dispatches `ADD_ATTACHMENT`.
- [PASS] Attachment chips render from `attachments` state with remove support (`REMOVE_ATTACHMENT`).
- [PASS] Send includes `images: attachments`; optimistic user message stores mapped image data URLs.
- [PASS] `UserMessage` renders image thumbnails from `message.images`.

7) Quota monitor flow (service emit -> provider post -> webview store -> quota cards)
- [PASS] `ChatViewProvider` subscribes to `QuotaService` `quotaUpdate` and posts `{ type: 'quotaData', data }`.
- [PASS] On `ready`, provider sends cached quota or triggers refresh.
- [PASS] `messageHandler` handles `quotaData|quotaUpdate` -> `SET_QUOTA_DATA`.
- [PASS] `store.ts` defines `SET_QUOTA_DATA` reducer branch.
- [PASS] `QuotaMonitor` renders platform cards/bars from `quotaData.platforms`.

8) Session CRUD flow (create/switch/delete -> list updates)
- [PASS] `HistorySidebar` posts `createSession`, `switchSession`, `deleteSession` with IDs.
- [PASS] Provider handles all three and calls session service (`createNewSession/switchSession/deleteSession`) and `handleGetSessions`.
- [PASS] Provider publishes `{ type: 'sessionsList', sessions, currentSessionId }`.
- [PASS] `messageHandler` consumes `sessionsList` and dispatches `SET_SESSIONS_LIST` + `SET_SESSION_ID`.
- [PASS] `store.ts` defines both actions and reducer branches.

Protected Feature Verification
- [PASS] `enrichMessageWithPlan` exists in `src/providers/ChatViewProvider.ts` with FORBIDDEN TO REMOVE note.
- [PASS] Plan button + plan card exist in `webview/shared/src/chat/MessageComponents.tsx` (core feature labels/tooltips present).
- [PASS] Sticky header exists in `webview/shared/src/chat/ChatShell.tsx` and renders `StickyHeader` with FORBIDDEN TO REMOVE marker.
- [PASS] `handleStopRequest` exists in `src/providers/ChatViewProvider.ts` with FORBIDDEN TO REMOVE note and `stopRequest` message path.
- [PASS] React Asset Contract verified in `ChatViewProvider.getHtmlContent`: includes `<div id="root"></div>`, `chat.js`, and `chat.css`.

Message/Action/Command Integrity Checks
- [PASS] Message type strings are consistent across webview emitters and provider handlers for all verified flows.
- [PASS] Required state actions used by handlers are defined in `store.ts`.
- [PASS] Required command registrations in `extension.ts` exist for plan open/proceed path (`opencode.showPlan`, `opencode.planProceed`).
- [PASS] No wiring-level type mismatch indicators found in reviewed integration paths.

Scenarios [8/8] | Integration [8/8] | Edge Cases [0] | VERDICT: PASS
