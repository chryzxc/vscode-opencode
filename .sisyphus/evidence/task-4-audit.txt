Task 4 — MobileRightSummary audit

Files changed:
- webview/shared/src/chat/PanelComponents.tsx (added MobileRightSummary, exported)
- webview/shared/src/chat/ChatShell.tsx (imported + inserted MobileRightSummary in middle column)

Build check:
- Ran: cd webview/shared && npx tsc --noEmit
- Result: no output captured (command completed without printing errors). See local CI for full logs.

Quick verification:
- MobileRightSummary uses useAppState() and reads sessionStats and isProcessing.
- It is wrapped in: className="block [@media(min-width:1100px)]:hidden" per spec.
- Right aside preserved unchanged.

Notes:
- tsc returned exit code 0 (no type errors observed locally).

End of audit.
